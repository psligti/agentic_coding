# OpenCode Frontend Environment Variables
# Copy this file to .env or .env.local and fill in your values
#
# NOTE: Vite only loads environment variables that start with VITE_
# These variables are embedded into the client bundle during build time
# DO NOT store secrets in VITE_ variables as they will be visible in the browser!

# =============================================================================
# API Configuration
# =============================================================================

# Base URL for the backend API
# This is the URL where the frontend will send API requests
#
# Development example: http://localhost:8000/api/v1
# Production example: https://api.example.com/api/v1
#
# Default (if not set): http://localhost:8000/api/v1
VITE_API_BASE_URL=http://localhost:8000/api/v1

# =============================================================================
# Optional: Feature Flags
# =============================================================================

# Enable debug mode in the UI (shows additional diagnostic information)
# Default: false
VITE_DEBUG=false

# Enable experimental features
# Default: false
VITE_EXPERIMENTAL=false

# =============================================================================
# Notes on Environment Variable Files
# =============================================================================

# Vite loads .env files in this order (later files override earlier ones):
#   1. .env                - Loaded in all environments
#   2. .env.local           - Loaded in all environments, ignored by git
#   3. .env.[mode]         - Loaded in specific mode (e.g., .env.development)
#   4. .env.[mode].local    - Loaded in specific mode, ignored by git
#
# Recommended practice:
#   - Commit .env and .env.example with safe defaults
#   - Add .env.local to .gitignore for local overrides
#   - Use .env.production for production-specific values
#   - Add VITE_ variables to frontend/src/vite-env.d.ts for TypeScript support
